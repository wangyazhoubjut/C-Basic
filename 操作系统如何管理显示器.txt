计算机如何让外设工作？

显卡：  cpu发送指令到显卡寄存器  写内容 卡自己计算 然后显示内容  中断处理

实际例子：
printf 其实就是一段操纵外设的程序

  open read write close  不同设备文件 dev/xx
  内部分支不同外设  out指令到显示器  先写到一个缓冲区 调用函数 往显存里面写
  printf("host name %s",name);
  write(1,buf,...)-> sys_write() 文件描述符


  设备驱动：写一些函数注册出去 根据设备文件 分支 创建dev啥的  放到缓冲区队列

  然后将队列中数据取到显存 用到了同步机制


  //操作系统如何使用键盘

  通过中断   21号中断处理函数





  